package ui

/*
type Row struct {
	Properties Properties
	Children   []UIElement
}

func (row Row) Draw(screen []byte) {
	row.Properties.Draw(screen)

	for child := range row.Children {
		//row.Children[child].GetProperties(row.Properties.Size)
		row.Children[child].Draw(screen)
	}

}

func (row Row) GetProperties(size Size, center Point) {
	row.Properties.MaxSize = size
}
*/
